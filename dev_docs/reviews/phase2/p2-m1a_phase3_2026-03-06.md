# P2-M1a Gate G2 Final Review

- Date: 2026-03-06
- Reviewer: tester
- Gate: p2m1a-g2 (phase 3)
- Target commit: `898b51321082c3a826a528e8149c79eb7bd1910d`
- Plan: `dev_docs/plans/phase2/p2-m1a_growth-governance-kernel_2026-03-06.md`

## Conclusion: PASS

All acceptance criteria are met. No P0 or P1 findings. Full test suite green (excluding pre-existing DB-dependent integration tests that require PostgreSQL). No boundary violations detected.

## Findings

No P0 or P1 findings.

### P2 (Informational)

| ID | Severity | File | Line | Description |
|----|----------|------|------|-------------|
| F1 | P2 | `src/growth/types.py` | 60 | `GrowthProposal.payload` typed as `dict[str, object]` -- loose typing. Acceptable for P2-M1a thin-wrapper pattern; tighten if more adapters land. |
| F2 | P2 | `src/growth/engine.py` | 69 | `rollback(**kwargs)` passes through arbitrary keyword arguments. Works for soul adapter's `to_version` kwarg. May need a more typed signature when additional adapters arrive in P2-M1b+. |

## Acceptance Criteria Evidence Matrix

| # | Criterion | Verdict | Evidence |
|---|-----------|---------|----------|
| AC1 | System can list allowed growth object kinds with onboarded/reserved annotation | PASS | `PolicyRegistry.list_kinds()` returns 5 `GrowthKindPolicy` instances. `test_policies.py::TestListKinds::test_returns_five_kinds` + `test_all_enum_members_covered` verify exhaustive coverage. `GrowthGovernanceEngine.list_supported_kinds()` delegates correctly, verified by `test_engine.py::TestListSupportedKinds::test_returns_registry_list`. |
| AC2 | SOUL.md governance via new kernel: propose/evaluate/apply/rollback/veto with unchanged observable semantics | PASS | `SoulGovernedObjectAdapter` delegates all ops to `EvolutionEngine` without modification. `test_soul_adapter.py` verifies all 6 delegation paths (propose, evaluate, apply, rollback, veto, get_active). `EvolutionEngine` itself is unmodified (`git diff origin/main -- src/memory/evolution.py` = empty). Existing regression tests `tests/test_evolution.py` (22 tests) and `tests/test_soul_tools.py` (21 tests) all pass. |
| AC3 | Governance kernel rejects un-onboarded objects with explicit error | PASS | `_require_adapter()` checks `onboarding_state != onboarded` and raises `UnsupportedGrowthObjectError(code="UNSUPPORTED_GROWTH_OBJECT")`. `test_engine.py::TestUnsupportedKindReserved` parametrically tests all 4 reserved kinds across all 6 operations (24 test cases). `TestOnboardedButNoAdapter` covers the adapter-missing case. |
| AC4 | Reserved promotion policy passes schema-level validation | PASS | `PromotionPolicy` dataclass instantiates with typed fields (`from_kind`, `to_kind`, `required_evidence`, `required_tests`, `risk_gate`). `test_policies.py::TestPromotionPolicies` (5 tests) validates type correctness, field access, serialisation, list count, and copy isolation. |
| AC5 | Existing SOUL.md regression tests do not degrade | PASS | `tests/test_evolution.py`: 22/22 passed. `tests/test_soul_tools.py`: 21/21 passed. Integration test `tests/integration/test_evolution_e2e.py` errors only due to missing PostgreSQL connection (pre-existing infrastructure constraint, not a regression). |
| AC6 | Full test suite green | PASS | 913 tests collected, 849 passed, 64 errors. All 64 errors are pre-existing DB-dependent integration tests (`tests/integration/`, `tests/test_ensure_schema.py`) that require a running PostgreSQL instance -- not available in the worktree environment. Zero failures among runnable tests. |

## Boundary / Non-Goal Compliance

| Check | Result | Evidence |
|-------|--------|----------|
| No `PromptBuilder` integration | PASS | `grep -rn "PromptBuilder\|prompt_builder" src/growth/` = no matches |
| No skill runtime code | PASS | `grep -rn "skill_runtime" src/growth/` = no matches |
| No DB migration | PASS | `git diff origin/main --name-only -- alembic/` = empty |
| No `evaluated` / `rejected` lifecycle status | PASS | `grep -rn '"evaluated"\|"rejected"' src/growth/` = no matches. Only match is log event name `growth_evaluated` (not a lifecycle status). |
| `src/memory/evolution.py` unmodified | PASS | `git diff origin/main -- src/memory/evolution.py` = empty |
| No unexpected file changes | PASS | Only files in `src/growth/`, `tests/growth/`, and `.beads/backup/` changed vs main |

## Test Summary

| Category | Count |
|----------|-------|
| Total collected | 913 |
| Passed | 849 |
| Errors (pre-existing, DB-dependent) | 64 |
| Failed | 0 |
| New growth tests | 68 |
| Regression tests verified (test_evolution.py) | 22 |
| Regression tests verified (test_soul_tools.py) | 21 |
| Lint | All checks passed (ruff) |

### New test breakdown

- `tests/growth/test_policies.py`: 18 tests (list_kinds, get_kind_policy x5 kinds, promotion policies, invalid kind, copy isolation)
- `tests/growth/test_engine.py`: 34 tests (construction, propose/evaluate/apply/rollback/veto/get_active delegation, list_supported_kinds, 24 parametric unsupported-kind rejection, onboarded-but-no-adapter)
- `tests/growth/test_soul_adapter.py`: 16 tests (kind property, Protocol conformance, propose conversion + payload validation + evidence forwarding, evaluate conversion, apply/rollback/veto/get_active delegation, None return)

All 68 new tests use mocks (AsyncMock) -- no DB or API consumption.

## Residual Risks

1. **DB-dependent integration tests not verified in worktree** (LOW): The 64 errored tests are all pre-existing integration tests requiring PostgreSQL. Since `src/memory/evolution.py` is completely unmodified and the growth module introduces no DB schema changes, regression risk is minimal.
2. **Loose `payload` typing** (LOW): `GrowthProposal.payload` is `dict[str, object]`. Acceptable for P2-M1a where only `SoulGovernedObjectAdapter` consumes it, but should be tightened when more adapters land.

## Gate Recommendation

**PASS** -- recommend gate close.

All 5 acceptance criteria are met with evidence. No P0/P1 findings. 68 new tests cover the full governance kernel surface. Existing SOUL.md regression tests are green. No boundary violations. Implementation is minimal and well-scoped to the approved plan.
